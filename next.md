- update name to first_name and lastName to last_name for both kids and guardians everywhere in the codebase and schema. 
- Provide a button to download all the emails of all guardians in the form of csv with columns: "first_name, last_name, email".
- Provide a PDF of a sample printable class list with kids and their guardians.
- update createdAt to created_at and updatedAt to updated_at in the schema and ensure compatibility with code where camel case is used.
- maintain an index of distinct firstNames, another for lastNames.

The concept of school class should be introduced. Before starting entering kids, a class needs to be created. The class will have the properties as defined in the schema: school_name, class_name, school_year. The class should also have an autogenerated uuid (class_id). Update the schema accordingly to have this id. The class should live on a standalone table in indexdb, and should have a list of kid ids. It should be ensured that these ids always exist in the kids table. When deleting a kid from the kids table, it should be removed from this list. A class needs to be always in scope in order to CRUD kids. The selected class should be shown on the left hand panel above the kids as a formatted string. The class will be created using a modal window with a small form. The button to create a new class should exist on the top bar, along with a select control to select one of the existing classes. To delete a class it needs to be empty from kids. Import and export feature will only be feasible within the scope of school class.